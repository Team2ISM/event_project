@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model System.Collections.Generic.Dictionary<uint,team2project.Models.Event>
@using System.Globalization
@Styles.Render("~/Content/Home/List.css")

<div class="grid">
    <h1>@ViewBag.Title</h1>
    <table class="table hovered striped border bordered sortable-markers-on-left">
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Date</th>
                <th>Time</th>
                <th>Location</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var curEvent in Model)
            {
                var evnt = curEvent.Value;
                <tr>
                    <td>@Html.ActionLink(evnt.Title, "Details", new { evnt.Id})</td>
                    <td>@Html.ActionLink(evnt.Title, "Details", new { evnt.Id })</td>
                    <td>@Html.ActionLink(evnt.From.ToString("dd MMMM yyyy", new CultureInfo("en")), "Details", new { evnt.Id })</td>
                    <td>@Html.ActionLink(evnt.From.ToString("hh:mm") + @evnt.To.ToString("hh:mm"), "Details", new { evnt.Id })</td>
                    <td>@Html.ActionLink(evnt.Location, "Details", new { evnt.Id })</td>
                </tr>
            }
        </tbody>
    </table>
</div>
